; adapted from coffeescript examples

(*def http (require "http"))
(*def server
  (http.createServer
    (*fn [ req res ]
      (res.writeHeader 200 (*obj "Content-Type" "text/plain"))
      (res.write "Hello, World!\nI'm fnjs.\n")
      (res.end) )))

(server.listen 3000)
(console.log "server @ http://localhost:3000/ ...")
